import { Inter } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

const inter = Inter({ subsets: ["latin"] });

export const metadata = {
  title: "MH Lee Metal Works - Luxury Iron Doors & Windows",
  description: "Crafting architectural excellence in custom iron doors and windows since 1990. Experience the perfect blend of Asian craftsmanship and European design.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Navbar />
        {children}
        <Footer />
      </body>
    </html>
  );
}
